version: 2
formats: []

build:
  image: latest

sphinx:
  configuration: docs/conf.py

python:
  version: "3.8"
  install:
    - method: pip
      path: .
      requirements: requirements.txt
      extra_requirements:
        - docs/requirementsRTD.txt